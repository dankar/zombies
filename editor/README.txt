README, Updated 2009-08-21
-----------------------------------------------------------------------------
The config directory (From the Resources directory) must be copied to the 
directory where the EXE is placed in when building & running the project,
for example bin/Debug/ or bin/Release.